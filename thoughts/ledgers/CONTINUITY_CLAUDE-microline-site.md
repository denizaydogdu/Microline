# CONTINUITY — microline-site

## Goal
Türkçe-öncelikli (EN seçmeli) Microline vitrin + sipariş formu sitesi. Spring Boot 3.5 + Thymeleaf + yerel PostgreSQL 14 (Docker YOK), Maven Wrapper + JDK 21. "Bitti" = rota tablosundaki tüm sayfalar iki dilde çalışıyor, formlar DB'ye yazıyor, sitemap/hreflang doğru, testler yeşil.

## Constraints
- Docker kullanılmayacak — yerel Homebrew postgresql@14 (dev: `microline`, test: `microline_test`)
- JDK 21 zorunlu (sistem 17) — `export JAVA_HOME=$(/usr/libexec/java_home -v 21)`
- Sepet/ödeme/üyelik YOK (v2); şema engellemeyecek
- Türkçe varsayılan/fallback; `_tr` kolonlar NOT NULL
- Flyway şemanın sahibi (`ddl-auto=validate`); seed = prod içerik
- Tasarım: framework'süz CSS, 8px grid token'ları (CLAUDE.md'deki kurallar)

## Key Decisions
- URL-path tabanlı dil (`/tr/...`, `/en/...`), `PathLocaleResolver` + `PageRoute` enum + çift `@GetMapping` (SEO/hreflang için; cookie/?lang= reddedildi)
- `_tr`/`_en` kolon çiftleri (translation table reddedildi — 2 sabit dil)
- Yasal sayfalar DB'de, `/{lang:tr|en}/{slug}` fallback route
- Testcontainers yerine yerel `microline_test` DB (Docker yok)
- Spring Boot parent 3.5.7; Lombok yok, DTO = record

## State
- Done:
  - [x] Phase 1: PostgreSQL başlatıldı, rol+DB'ler oluşturuldu, git init, pom.xml + Maven Wrapper, application*.yml, MicrolineApplication, CLAUDE.md, README
  - [x] Phase 2: PathLocaleResolver, PageRoute, LocaleUrls, WebConfig, SecurityConfig (CSP/XFO/referrer, /admin denyAll), GlobalModelAdvice, HomeController (/ → /tr/), messages TR/EN, base.html + header/footer fragment'ları, tokens/base/layout/components/pages.css, menu.js, home iki dilde, localize 404/500 — curl ile doğrulandı (302, TR/EN render, asset'ler 200, header'lar)
  - [x] Phase 3: BaseEntity + JpaConfig (auditing), 10 entity + I18n.pick (TR fallback) + 3 enum (CollectionCode/Difficulty/InquiryStatus), 10 repository, V1__schema.sql + V2 seed (4 koleksiyon, ML-C1 amiral gemisi + 2 varyant + 3 görsel, ML-A1/ML-K1/ML-B1), MicrolineApplicationTests (@ActiveProfiles("test") + FlywayMigrationStrategy clean+migrate) — `./mvnw test` yeşil (2/2), dev DB'de V1+V2 uygulandı, psql ile 4 koleksiyon doğrulandı
  - [x] Phase 4: CollectionService + ProductService (@Transactional readOnly; open-in-view=false → görsel/varyant/yorum ayrı sorgular), ShopController (çift slug mapping, bilinmeyen slug → ResponseStatusException 404, model'e açık alternateUrl), header dil değiştirici alternateUrl > route önceliği, mega-menü navCollections (GlobalModelAdvice → DB), LocaleUrls.collectionPath/productPath (EN slug yoksa TR fallback), Prices bean ("4.990 ₺" / "₺4,990"), collection.html + product.html (galeri placeholder, buy box, varyant radio kartları, specs tablosu — specsJson Jackson ile Map'e, boolean → common.yes/no, liste → ", "), tam ana sayfa (hero çift CTA, güven şeridi, 3 adım, öne çıkanlar, bundle teaser, bülten form kabuğu → /tr/bulten-abonelik POST Phase 6'da), product.js (galeri + varyant fiyatı; fiyat sunucuda biçimlenir, data-variant-price), components/pages.css genişletildi, mesajlar TR/EN. ÖNEMLİ FIX: SecurityConfig'e CsrfFilter sonrası erken token çözümü eklendi (deferred CSRF, streaming Thymeleaf commit sonrası session yaratamıyordu → yarım sayfa); footer.copyright {0,number,#} ("2.026" düzeltildi). curl ile iki dilde doğrulandı, testler yeşil
  - [x] Phase 5: PageController (hakkımızda/eğitimciler/ürün-güvenliği/yeşil-gelecek/ortaklık), ReviewController + ReviewService (özet: ortalama/adet/%5yıldız, dil örneklemi <3 ise tüm onaylılardan; ReviewStats record dto), TutorialController + TutorialService (hub + detay, detayda açık alternateUrl — LocaleUrls.tutorialPath eklendi, bilinmeyen slug 404, CTA → amiral gemisi ürün, publishedAt Europe/Istanbul + dile göre biçimlenir), V3__seed_reviews_and_tutorials.sql (10 yorum: 7 tr + 3 en, 3 featured, biri 4 yıldız, ML-C1'e bağlı + genel; 5 eğitim: 2 BEGINNER/2 INTERMEDIATE/1 ADVANCED, iki dilli slug/başlık/özet/HTML gövde, created_at deterministik sıralama için açık). 8 şablon (about timeline + mission band + değerler; educators 3 kart + toplu alım tablosu %10/%15 + testimonial + rozet şeridi + CTA; product-safety sertifika kartları + stat bandı (yer tutucu) + rapor CTA; sustainability fidan sözü + 3 adım + döngüsellik + mağaza CTA (CUTTING_TOOLS koleksiyonu); affiliate avantajlar + 3 adım + mailto CTA; reviews özet başlık + featured alıntı şeridi + tam liste; tutorials kart grid'i; tutorial-detail meta + utext gövde + geri link + CTA). Mesaj anahtarları iki bundle'da; footer'a Ürün Güvenliği + Yeşil Gelecek linkleri eklendi (nav.sustainability). CSS: page-hero/cta-band/timeline/mission-band/pricing-table/testimonial/cert-issuer/stat-band/review-aggregate/quote-strip/tutorial kartları + zorluk rozetleri (yeni level token'ları). ÖNEMLİ FIX: #strings.toLowerCase Türkçe locale'de I→ı yapıp CSS sınıfını bozuyordu ("difficulty-begınner") → Difficulty.cssSuffix() (Locale.ROOT). 14 statik rota + detaylar curl ile iki dilde doğrulandı, TR↔EN detay round-trip, bilinmeyen slug 404, testler yeşil.
  - [x] Phase 6: DTO record'ları (OrderRequestForm/ContactForm/NewsletterForm; validator MessageSource'a bağlandı — WebConfig validatorFactory + getValidator), InquiryService (honeypot + 3sn min gönderim süresi + IP throttle 5/10dk bellek-içi, spam'e sessiz başarı; bülten idempotent + unsubscribe sonrası yeniden aktivasyon), OrderRequestController (?urun= önseçim + varyant listesi, PRG + flash formSuccess) + ContactController + NewsletterController (redirect whitelist /tr/ /en/, open-redirect reddi), order-request.html + contact.html (hp-field honeypot, formRenderedAt hidden, alan hataları, KVKK checkbox + link) + home bülten formu gerçek alanlarla, forms.js (çift gönderim engeli), form CSS + --success/--error token'ları, ProductService.byId/activeProducts, TestFlywayConfig paylaşılan test config'e çıkarıldı, FormSubmissionTests 8 test (CSRF'siz 403, KVKK'sız alan hatası + kayıt yok, honeypot 302 ama kayıt yok, idempotent bülten, off-site redirect → /tr/) — 10/10 test yeşil; canlı curl: geçerli POST → DB satırı, honeypot → satır yok, bülten → abone kaydı (psql ile doğrulandı)
  - [x] Phase 7: LegalPageService + LegalPageController (/{lang:tr|en}/{slug} fallback — literal rotalar önce eşleşir, bilinmeyen slug 404), legal.html (noindex, EN gövde boşsa TR + uyarı bandı), V4__seed_legal_pages.sql (9 sayfa, dollar-quoted, LAWYER REVIEW REQUIRED işaretli, [PLACEHOLDER] alanlar; garanti asgari 2 yıl m.6/1, cayma 14 gün + 1 yıl uzama, 30 gün teslimat, KVKK m.11, 12 punto notu), cookie-consent fragment + cookie-consent.js (localStorage microline-consent-v1, analytics bayrağı gelecek için) + base.html include, legal/cookie CSS + mesajlar — 18 yasal URL + 404 + banner curl ile doğrulandı, 10/10 test yeşil
  - [x] Görseller (Higgsfield MCP, nano_banana_pro/2, 19 üretim = 38 kredi, kalan 82): logo (brand/logo.png + header için kırpılmış logo-header.png), home hero (pages/hero-home.jpg), 5 ürün görseli (microline-1/2/3 — 2 ve 3 ilk görseli referans alarak tutarlı seri, prokesim-1, baslangic-kiti-1, set-1), 4 koleksiyon banner'ı, 5 eğitim kapağı (tutorial/), atölye + sınıf (pages/). PNG'ler sips ile gerçek JPEG'e çevrildi (33MB → 3.9MB). Şablon bağlama: header logo img, product-card hero img, ürün galerisi gerçek img + product.js src-swap, collection hero 2 kolon + img, home hero 2 kolon + img, tutorial kart kapakları, about/educators page-media; V5__tutorial_covers.sql (cover_image_url UPDATE'leri). Tüm img'ler 200 + doğru content-type, 10/10 test yeşil.
  - [x] Code review (3 ajan) + 11 düzeltme: (1) PathLocaleResolver hata dispatch'inde jakarta.servlet.error.request_uri'ye bakıyor — EN 404/500 artık İngilizce; (2) GlobalModelAdvice.navCollections try-catch + boş liste (DB çökerken hata sayfası ayakta); (3) yinelenen .collection-hero-inner silindi + :not(:has(img)) tek sütun fallback; (4) throttle synchronized + pruneStaleIps (yarış + map büyümesi) + microline.throttle.max-per-window property (test profili 1000); (5) forward-headers-strategy varsayılan none, application-prod.yml: framework + 127.0.0.1 bind (DigitalOcean+nginx; README'de nginx örneği); (6) safeTarget string-tabanlı sertleştirme (\\, .., //, : reddi — UriComponentsBuilder kenar durumu yüzünden parser'dan vazgeçildi) + 5 varyantlı regresyon testi; (7) logo width=114 height=40 (CLS); (8-9) V6__indexes_and_kvkk_check.sql (3 indeks + kvkk_consent CHECK); (10) forms.js senkron submitting bayrağı; (11) çerez banner'ı focus(). 11/11 test yeşil, canlı doğrulandı.
  - [x] Sepet özelliği (3 Plan ajanı tasarımı, plan onaylı): V7__cart_orders.sql (order_request_items: product/variant FK + quantity CHECK + unit_price/currency snapshot; mevcut satırlar taşındı; order_requests'ten product_id/variant_id/quantity DROP), OrderRequestItem entity + OrderRequest.items (cascade ALL + orphanRemoval) + addItem helper, @SessionScope Cart (LineKey merge, synchronized) + CartService (aktiflik/aidiyet doğrulama, 20 satır cap, qty 1-999 clamp, view() pasif ürün budama + removedAny), CheckoutForm (contact-only — satırlar session'dan) + CartAddForm (validasyonsuz, servis clamp'ler), InquiryService.saveOrderRequest(form, lines, ...) Result.EMPTY_CART eklendi, PageRoute.CART("sepet","cart"), CartController (GET sepet + POST ekle/guncelle/kaldir; ekleme PRG → ürün#cart-banner), OrderRequestController = checkout (boş sepet → sepete redirect + flash, ?urun= → ürüne 302, SAVED/SPAM'de sepet temizlenir + orderedCount/orderedTotal flash), GlobalModelAdvice.cartCount (try-catch), header .cart-link (SVG + rozet, nav dışında), product.html buy-box form'a dönüştü (variantId radio + qty + Sepete Ekle; product.js selector güncellendi), yeni cart.html (satır başına update/remove formları, sticky özet, boş durum, uyarı bantları), order-request.html checkout özeti + başarı ekosu, ~27 mesaj anahtarı ×2 + 5 ölü anahtar silindi, --radius-full token + cart CSS bloğu. FormSubmissionTests sepet akışına uyarlandı (paylaşılan MockHttpSession; PRG sonrası ilk GET 200/flash, ikincisi redirect) + CartFlowTests 8 test (merge, varyant delta fiyat, update/remove, tamper no-op, yabancı varyant reddi, clamp 999, EN sayfa) — 22/22 yeşil. Canlı doğrulama: 2 ürün ekle → toplam 10.970 ₺ → qty güncelle 5.980 ₺ → submit → DB'de 2 item satırı (4990/990 TRY snapshot) → sepet boş, rozet yok; /en/cart, eski ?urun= 302, boş sepet checkout redirect, dil değiştirici /tr/sepet ↔ /en/cart.
  - [x] Mobil uyumluluk düzeltmeleri (kullanıcı bildirimi + UI review bulguları, Playwright JS ölçümüyle doğrulandı): (1) tarayıcı doğrulama balonları yerelleştirildi — body data-msg-* attribute'ları (base.html) + forms.js invalid/input capture handler'ları (valueMissing/typeMismatch/range → validation.* mesajları; checkbox → kvkk mesajı); KVKK checkbox'larına required eklendi (contact + order-request); canlıda "Bu alan zorunludur." doğrulandı. (2) Fiyat kırılması: .price/.price-compare/özet+toplam fiyatlarına white-space:nowrap — "14.970 ₺" tek satır (390px ölçüldü). (3) Hamburger: Menü metni → 3 çizgili ikon (.menu-toggle-bars, 44×44), mobil header sırası logo solda / sepet+hamburger sağda (order + margin-inline-start:auto). (4) iOS zoom: input fontları mobilde 16px — DİKKAT: kural dosya SONUNDA ayrı media bloğunda olmak zorunda (eşit specificity, kaynak sırası kazanır; ilk deneme erken blokta ezildi). (5) Sepet satırı mobil grid-template-areas (info+total üst, qty orta, remove alt — ölçümle doğrulandı). (6) Dokunma hedefleri: nav-link 52px, lang-switch/cart-link/menu-toggle/btn-sm 44px. (7) 720px→768px breakpoint birleştirme (contact-grid, newsletter-row, hero-with-media, collection-hero-inner — 740px'te contact artık tek sütun, ölçüldü). (8) page-hero mobil padding space-6, gallery-thumbs flex-wrap, KVKK checkbox 24px + link underline, drawer max-height:100dvh+overflow-y, body 100dvh. NOT: "Mağaza dropdown hover-only/touch'ta ölü" iddiası YANLIŞTI — menu.js tıklama tabanlı, canlı testte drawer+dropdown tap ile açıldı, 4 koleksiyon linki erişilebilir. 22/22 test yeşil. Ekran görüntüsü PNG'leri proje kökünden temizlendi.
  - [x] Son code review (3 ajan) + 7 düzeltme: (1) forms.js bfcache — pageshow persisted'da submitting bayrağı + buton resetlenir (Geri tuşu sonrası ölü form); (2) base.html noscript fallback — JS yokken .site-nav display:block (progressive enhancement ihlali kapandı); (3) forms.js change capture dinleyicisi — autofill customError'ı temizler; (4) localizedMessage'a badInput dalı (sayı alanında doğru mesaj); (5) .site-nav.is-open'a 100vh fallback satırı (dvh öncesi); (6) checkbox margin-top 4px→0 (24px kutuya göre); (7) input fontları KOŞULSUZ 16px (--text-base) temel kurallarda — kırılgan dosya-sonu media bloğu silindi (iOS zoom kökten çözüldü). Review'da temiz çıkanlar: Thymeleaf yorum sızıntısı yok, dataset eşlemesi doğru, grid-areas↔DOM birebir, i18n parite tam, .price nowrap taşma riski konteyner konteyner doğrulandı. 22/22 test yeşil; canlı doğrulama (noscript sayfada, vh fallback + 16px kural servis ediliyor, eski blok kalktı, forms.js yeni handler'lar) sonrası uygulama kullanıcı isteğiyle DURDURULDU.
  - [x] Phase 8 — SEO: SitemapService (PageRoute×2 dil + aktif ürün/koleksiyon/eğitim slug'ları, xhtml:link tr/en/x-default alternate'leri; CART+ORDER_REQUEST+yasal hariç) + SeoController (/sitemap.xml, /robots.txt — işlemsel sayfalara Disallow), GlobalModelAdvice baseUrl+canonicalUrl (query'siz request URI), base.html head canonical/hreflang bloğu (DİKKAT: th:if öncelik 300 < th:with 600 — iç içe th:block şart, ilk deneme bu yüzden boş render etti) + OG site_name/locale/image + twitter:card + favicon link, product.html OG + Product/Offer/AggregateRating JSON-LD (CSP ld+json'u engellemez — çalıştırılmaz), home Organization JSON-LD, order-request noindex. SeoTests 4 test.
  - [x] UI cila: base.css tip ölçeği h1 48/h2 36/h3 24 hepsi 700 + mobilde bir kademe iner; tüm font-weight:600 → 500; .form > .btn sola hizalı min 280px (mobilde stretch); hero 2 kolonda sola hizalı metin; çerez banner'ı açık tema (gray-900 → color-bg + border-top); zorluk rozetleri nötr gri pill + semantik renkli nokta (::before); kart hover lift (translateY -2px) + padding space-3; legal 640px (~72ch) + h2 text-xl; cart-empty space-12; tek ürünlü koleksiyon grid'i 380px'e kapanır (:has(:only-child)); iç bölümler space-8 ritmi.
  - [x] Dönüşüm kopyası: cart.checkout.note/form.order.intro/form.order.success.body "fiyat belli değil" iması → "gördüğünüz fiyat geçerli + telefonla arayıp ödeme/teslimatı tamamlarız"; yeni product.noPayment.note CTA altında; boş sepet CTA'sı ana sayfa yerine kesim araçları koleksiyonuna (CartController shopUrl).
  - [x] Görseller: 3 bozuk yazılı eğitim kapağı (kutu-ev, robot-maske, bilye-parkuru) + "SHOP NOW" yazılı koleksiyon hero'su metinsiz yeniden üretildi (STRICTLY NO text prompt'ları işe yaradı) + 64px favicon (amber kare zigzag M) — 10 kredi; tutorial-detail.html artık coverImageUrl render ediyor (gri placeholder kalktı). 26/26 test yeşil; sitemap/robots/canonical/JSON-LD/favicon canlı doğrulandı.
  - [x] GitHub: ilk commit e6ae52f (140 dosya, /commit skill — Claude imzasız) + origin https://github.com/denizaydogdu/Microline.git push edildi. NOT: push'u auto-mode sınıflandırıcısı engelledi (farklı hesaba public repo = exfiltration şüphesi) — remote'u ben ekledim, push'u kullanıcı kendi terminalinden yaptı. .gitignore'a .agents/.claude/skills-lock.json/.playwright-mcp eklendi.
- Backoffice (plan: microline-website-blueprint-prancy-peach.md "Uygulama Sırası"):
  - [x] Phase A — Kimlik temeli: V8__admin_users.sql (seed'siz) + AdminUser/AdminUserRepository + AdminUserDetailsService (tek UserDetailsService, ROLE_ADMIN) + AdminUserInitializer (tablo boşsa env şifresiyle/rastgele WARN'lı admin) + SecurityConfig İKİ chain (@Order(1) /admin/** formLogin /admin/giris + logout /admin/cikis + changeSessionId; @Order(2) public — denyAll kalktı; eager-CSRF + CSP/header'lar paylaşılan private metodlarda, BCrypt PasswordEncoder bean) + GlobalModelAdvice daraltıldı (basePackageClasses=HomeController + assignableTypes=ErrorController — SAPMA: ErrorController eklenmeseydi 404/500 sayfaları header'daki routes/navCollections'sız 500 verirdi, canlıda doğrulandı) + AdminModelAdvice (adminUserName + NEW sayaçları; countByStatus×2 + countByUnsubscribedAtIsNull + findTop5ByOrderByCreatedAtDesc repo metodları eklendi) + AdminAuthController/AdminDashboardController (4 stat + son 5 talep, Instant controller'da Europe/Istanbul ile biçimlenir)/AdminAccountController + AdminAccountService + PasswordChangeForm (record, min 10) + admin-base/sidebar/login/dashboard/password şablonları (hepsi #{admin.*}) + admin.css (240px sidebar, ≤900px off-canvas, status-badge nokta reçetesi) + admin.js (toggle + data-confirm) + ~75 admin.* TR anahtarı + application.yml microline.admin.* (test: initial-password=test-admin-pass) + AdminSecurityTests 9 test (@BeforeEach idempotent admin tohumu — başka context'in clean'i satırı silerse diye). 35/35 test yeşil; canlı doğrulandı: /admin→302 giriş, curl ile CSRF'li login→302 /admin→200 TR dashboard, şifre değiştirme round-trip (yeni şifreyle giriş + eskisi ?error), logout→?logout, /tr/ mega-menü + 404 sayfası sağlam. DİKKAT: dev'de devtools hot-restart initializer'ı env şifresi OLMADAN koşturabilir → rastgele şifre; çözüm: DELETE FROM admin_users + env ile temiz restart.
  - [x] Phase B — Upload altyapısı: microline.uploads-dir 3 profilde (yml ./uploads gitignore'lu, prod /var/microline/uploads + yedekleme/nginx notu, test target/test-uploads) + multipart limitleri yml'de (max-file-size 5MB / max-request-size 12MB), admin/service/ImageStorageService (nested ImageKind enum product/collection/tutorial; store(): boş→boyut→beyan allowlist→ilk 12 bayt magic sniff (JPEG FF D8 FF / PNG 8-bayt / RIFF+WEBP) — uzantıyı SNIFF belirler, beyan↔sniff uyuşmazlığı red; dosya adı UUID.ext; deleteIfUploaded(): yalnız /uploads/ prefix'i, normalize + startsWith(root) traversal koruması, /img/ seed'lerine dokunmaz; @PostConstruct kök+3 alt dizin) + ImageStorageException (messageKey + Türkçe mesaj — Phase C'de controller'lar alan hatasına çevirecek) + WebConfig.addResourceHandlers /uploads/** → file: URI (sondaki "/" zorunlu, setCachePeriod 3600; @Value uploads-dir constructor'a eklendi). ImageStorageTests 12 test (gerçek JPEG/PNG magic byte'larıyla store + dosya varlığı, HTML-as-jpg red + dizin sayısı değişmedi, text/html red, png-beyan/jpeg-içerik mismatch red, 6MB red, boş red, delete round-trip, /img/ no-op, traversal nöbetçi-dosya testi, MockMvc /uploads/... 200 image/jpeg + /uploads/yok.jpg 404≠401). 47/47 test yeşil.
  - [x] Phase C — Katalog: i18n/Slugs.slugify (TR harita ÖNCE ı/İ→i ş→s ğ→g ç→c ö→o ü→u, sonra toLowerCase(Locale.ROOT) — TR I→ı tuzağı yorumda, NFD+\p{M} strip, non-[a-z0-9] koşuları→tek tire, kenar tireler kırpılır, null/boş→"") + repo ekleri (Product: @EntityGraph(collection) findAllByOrderBySortOrderAscIdAsc — liste koleksiyon adını basar, open-in-view=false'ta lazy patlardı + existsBySku/SlugTr/SlugEnAndIdNot, create'te id yerine -1L geçilir; Collection: findAllByOrderBySortOrderAscIdAsc + existsBySlug*AndIdNot; Variant: findByProductOrderBySortOrderAscIdAsc + existsBySkuAndIdNot) + admin/dto/{ProductForm,VariantForm,CollectionForm,ProductImageForm,ImageMetaForm} (record + validation; slug pattern ^$|^[a-z0-9]+(-[a-z0-9]+)*$). ÖNEMLİ BULGU: checkbox boolean'ları WRAPPER Boolean olmalı — record constructor binding'de eksik parametre null→primitive typeMismatch üretir VE model attr null kalır (şablon re-render "null bean instance" 500'ü); serviste Boolean.TRUE.equals ile kopyalanır. AdminCatalogService (@Transactional; slug boşsa addan üretim — slugEn yalnız EN ad varsa türetilir yoksa NULL; benzersizlik + specsJson readTree → rejectValue, save yalnız bindingResult temizse; koleksiyon hero replace: yeni dosya→save→SONRA deleteIfUploaded(eski); addImage sortOrder=max+1; deleteImage satır+dosya; setHero product.heroImageUrl=image.url; ownedVariant/ownedImage sahiplik filtresi → 404) + AdminCollectionController (liste/düzenle GET+POST multipart/durum; create YOK — 4 sabit enum kod) + AdminProductController (liste, yeni, düzenle, durum, varyant ekle/güncelle/durum, görsel yükle (multipart)/meta/kapak/sil; ana form + EKLE formları hatada alan-hatalı re-render, satır mini-formları (varyant güncelle, görsel meta) flashError ANAHTARI + PRG — şablon #{${flashError}}; ImageStorageException → rejectValue(e.messageKey)) + 4 şablon (admin/collections/list+form, admin/products/list+form: fieldset'ler "Türkçe içerik"/"English content (opsiyonel)"/"Fiyat ve katalog"/"SEO", slug hint + URL-kırılır uyarısı, .admin-html-input monospace textarea'lar, specsJson hint; düzenlemede varyant satırları = cart-line kalıbı bağımsız mini-form (hidden active taşınır — yoksa her satır kaydı varyantı pasifleştirirdi), görsel grid'i thumb+meta-form+kapak/sil+upload formu, danger zone toggle data-confirm'li) + admin.css yeni bloklar (admin-page-head/fieldset/form-hint/html-input/check-field/row-list/image-grid/upload-form/danger/btn-danger + ≤900px row dikey) + ~95 yeni admin.* anahtarı (f.*, form.section.*, validation.*, flash.created/uploaded/deleted/statusChanged/invalid, image.error.* — ImageStorageException messageKey'leriyle birebir, products.*, collections.*, danger.*) + AdminCatalogTests 14 test (user("admin").roles("ADMIN") post-processor — UserDetails bağımsız; GET sayfaları render testi (Thymeleaf hataları yalnız render'da çıkar); TR slug kanıtı "Yeni Test Şablonu"→"yeni-test-sablonu" + slugEn NULL; slug/sku/specsJson red + satır yazılmadı; fiyat düzenleme — DİKKAT: test DB'si context'ler arası paylaşılır, mutasyonlar seed'e geri döndürülür (fiyat, koleksiyon adı, hero URL'leri) yoksa Cart/SEO testleri kırılır; toggle round-trip activeProducts; negatif delta varyant; varyant SKU çakışması re-render; görsel yaşam döngüsü upload→meta→kapak→sil TEK testte (metod sırası bağımsızlığı); koleksiyon ad + hero upload /uploads/collection/; anonim POST→/admin/giris). 61/61 test yeşil.
  - [x] Phase D — İçerik: repo ekleri (TutorialPost: findAllByOrderByCreatedAtDescIdDesc — publishedAt taslakta null olduğundan createdAt'e sıralanır + existsBySlugTr/EnAndIdNot; Review: @EntityGraph(product) findAllByOrderByCreatedAtDesc — product LAZY + open-in-view=false, graph'sız admin listesi render'da patlardı + countByApprovedFalse Phase E pill'i için şimdiden; LegalPage: findAllByOrderByIdAsc) + admin/dto/{TutorialForm,LegalPageForm} (TutorialForm: slug pattern boş→başlıktan, excerptTr ≤500, bodyTr HTML, videoUrl ≤300 ops, difficulty @NotNull enum — Spring string→enum kendisi bağlar, published WRAPPER Boolean, coverImage MultipartFile ops; LegalPageForm yalnız title/body — code/slug bilinçli YOK, panelden URL değişmez). AdminContentService (katalogla aynı kurallar: resolveSlug/resolveOptionalSlug + -1L create konvansiyonu + rejectValue; kapak replace: store→save→SONRA deleteIfUploaded(eski); YAYIN KURALI: publishedAt yalnız İLK yayına geçişte Instant.now() damgalanır, kapat/aç döngüsü orijinal tarihi korur — vitrin sıralaması stabil; eğitim/yorum HARD delete (eğitimde kapak dosyası da gider); toggleFeatured onaysızken otomatik ONAYLAR — vitrin yalnız featured+approved bastığından aksi sessiz no-op olurdu, geri alma onaya dokunmaz; yasal: yalnız title/body update). AdminTutorialController (liste TutorialRow record'larıyla — Instant şablonda biçimlenemez, dd.MM.yyyy Europe/Istanbul controller'da, taslak '—'; difficulties model attr — şablonda T() expression yerine; create/update multipart, ImageStorageException→rejectValue(coverImage); durum/sil → liste'ye PRG) + AdminReviewController (yalnız liste + onay/one-cikar/sil toggle endpoint'leri — adanmış endpoint'ler olduğundan satır formlarında hidden input GEREKMEZ, varyant kalıbının aksine) + AdminLegalController (liste + düzenle; create/delete yok). 5 şablon: tutorials/list (difficulty-badge + cssSuffix — components.css admin'de de yüklü, public reçete aynen; yayın/taslak status-badge; satırda yayın toggle + data-confirm sil) + tutorials/form (TR/EN/Yayın-medya fieldset'leri, mevcut kapak thumbnail'i, danger zone toggle+sil) + reviews/list (yazar/puan/ürün-veya-'-'/dil/rozetler/#strings.abbreviate(body,80) + 3 mini-form) + legal/list (9 satır + vitrine Görüntüle linki) + legal/form (salt-okunur kod/slug bölümü + title/body + admin.legal.body.note 'avukat onayısız yayınlamayın' uyarısı). ~50 yeni admin.{tutorials,reviews,legal}.* TR anahtarı; difficulty.* mevcut anahtarlar yeniden kullanıldı (#messages.msg('difficulty.' + name())). AdminContentTests 9 test (render hepsi; anonim→giriş; TR slug 'Hızlı Başlangıç Şablonu'→'hizli-baslangic-sablonu' taslak+publishedAt null; yayın döngüsü damga-bir-kez/koru/üzerine-yazma; kapak upload /uploads/tutorial/ + sil→satır+dosya gitti; kopya slug re-render + count değişmedi; yorum yaşam döngüsü KENDİ kaydıyla (seed'e dokunmaz): onay→ReviewService.approved(tr)'ye düşer, geri al, onaysızken öne çıkar→featured+approved birlikte, sil; yasal gövde güncelle→repo + public /tr/garanti'de marker görünür (uçtan uca) sonra seed'e dönüş; boş bodyTr reddi). 70/70 test yeşil.
  - [x] Phase E — Gelen kutusu + panel: repo ekleri (OrderRequest: Page findAllByOrderByCreatedAtDesc + Page findByStatusOrderByCreatedAtDesc + @EntityGraph(items, items.product, items.variant) findWithItemsById — LAZY + open-in-view=false, graph'sız detay LazyInitException; ContactMessage: aynı iki Page metodu; NewsletterSubscriber: Page + List findAllByOrderByCreatedAtDesc overload'ları — List CSV tam dökümü için) + admin/dto/StatusForm (record, @NotNull InquiryStatus — geçersiz enum string'i binding typeMismatch) + AdminInboxService (@Transactional; içerik ASLA düzenlenmez/silinmez yalnız durum yazılır — KVKK kayıt bütünlüğü; orderRequestWithItems → OrderRequestDetail record: lines + lineTotal + genel toplam BigDecimal SERVİSTE hesaplanır, şablonda aritmetik yok; subscribersCsv: '﻿' BOM (Excel-TR yoksa Windows-1254 sanar) + email,locale,kvkk_consent_at,unsubscribed_at başlığı + ISO Instant'lar + RFC 4180 savunmacı tırnak escaper'ı). AdminInboxController (tek controller, 3 prefix: /admin/siparis-talepleri, /admin/mesajlar, /admin/aboneler; 20/sayfa PageRequest; ?durum= valueOf try/catch — geçersiz değer SESSİZCE filtresiz listeye düşer; satır record'ları RequestRow/MessageRow/SubscriberRow/ItemRow — tarih dd.MM.yyyy HH:mm Europe/Istanbul + fiyatlar Prices.format(PathLocaleResolver.TURKISH) CONTROLLER'da biçimlenir; StatusAction record'ları mevcut durum hariç iki geçiş butonu üretir (admin.requests.markNew/markContacted/markClosed — mesaj detayı da aynı anahtarları kullanır); POST durum: binding hatasında flashError=admin.flash.invalid + PRG detaya, kayıt DEĞİŞMEZ; CSV: ResponseEntity<byte[]> text/csv;charset=UTF-8 + attachment; filename="aboneler.csv"). Şablonlar: admin/fragments/inbox.html (pills + pagination fragment'ları PARAMETRESİZ — addListModel'in statusFilter/basePath/pageNumber/totalPages/hasPrev/hasNext attr'larını okur; href'te dinamik baz @{${basePath}(durum=...)} çalışır) + admin/inbox/{requests-list, request-detail (admin-detail-grid 1fr+320px: mesaj+kalemler solda / iletişim dl (mailto+tel linkli, IP, KVKK common.yes/no, locale) + durum bloğu sağda; kalem tablosu ürün adı /admin/urunler/{pid} linkli + tfoot genel toplam), messages-list, message-detail, subscribers (toolbar: toplam kayıt + CSV İndir btn)}. Dashboard finalize: ilk iki stat kartı ?durum=NEW linkli + count>0'da is-alert (accent border + accent değer), son talepler satır adları detaya linkli ("Tümünü gör" zaten vardı). admin.css: filter-pill/pagination/toolbar-note/detail-grid/detail-block/detail-meta dl/admin-pre (ziyaretçi mesajı pre-wrap)/status-actions/is-alert + ≤900px detail-grid tek sütun. ~50 yeni admin.{requests,messages,subscribers,pagination,filter}.* TR anahtarı (detailTitle'larda {0,number,#} — yoksa MessageFormat id'yi "1.234" basar, footer.copyright dersi). AdminInboxTests 7 test (repo-direct tohum — sepet akışı CartFlowTests'te zaten; her test kendi kaydını finally'de siler: liste+NEW görünür/CLOSED gizli/geçersiz durum yok sayılır; detay ürün adı+4.990 ₺+9.980 ₺+IP (@EntityGraph kanıtı); durum CONTACTED persist + geçersiz string'de redirect ama durum SABİT; mesaj liste/detay/durum; 25 mesaj→sayfa0 20+sayfa1 5 satır (saves arası Thread.sleep(2) — created_at eşitliği sayfa sınırı kaydırır); aboneler sayfası + CSV 200 text/csv BOM+başlık+satır+Content-Disposition; anonim→giriş). 77/77 test yeşil.
  - [→] Phase F — Sertleştirme + dokümantasyon: ./mvnw verify, CLAUDE.md güncellemeleri, prod uploads/nginx notu, canlı QA, commit+push
  - [x] Backoffice Phase F: CLAUDE.md'ye /admin bölümü (ilk admin env var, iki chain + paylaşılan eager-CSRF/CSP uyarısı, uploads-dir + yedekleme, XSS güven modeli değişimi, Boolean checkbox tuzağı, soft-delete kuralı, slug uyarısı), canlı QA (giriş 302→/admin, dashboard 4 stat, tüm liste/form sayfaları 200, CSV text/csv BOM, anonim→giriş 302, public /tr/ regresyonsuz) — 77/77 test, commit+push
  - [x] Backoffice code review (3 ajan, 2 round → 0 must-fix) + Chrome E2E + MD: Round 1 gerçek bulgular düzeltildi — (1) görsel silinince kapaksa hero sıradaki görsele taşınır/null'lanır (vitrinde kırık img yok); (2) password.html for/id eşleşmesi (th:field id'yi eziyordu); (3) noscript inline <style> → harici admin-nojs.css (CSP 'self'); (4) CSV formül enjeksiyonu (=,+,-,@ → ' önek + tırnak); (5) şifre değişiminde session invalidate + /admin/giris?sifre-degisti; (6) AdminModelAdvice login sayfasında sayaç sorgusu yapmıyor (Anonymous guard); (7) inbox sayfalama id tiebreaker (CreatedAtDescIdDesc); (8) 10 admin tablosu .admin-table-wrap ile sarıldı (mobil overflow-x). YANLIŞ ÇIKAN iddialar: "LazyInit her edit'te çöker" (Product.collection @ManyToOne default EAGER), "admin.messages.mark* eksik" (controller bilinçli requests.* paylaşıyor). Round 2: 0 must-fix. 78 test yeşil. Chrome E2E (Playwright): anonim /admin→giriş, admin giriş→dashboard 4 stat + NEW rozetleri, ürün fiyat düzenle 4990→5290 → DB → vitrin 5.290 ₺ yansıdı, yorumlar 10 satır + içerik escape, mobil drawer (data-menu-toggle + is-open transform:0 görünür), çıkış→session öldü, public regresyon 0 konsol hatası + mega-menü + JSON-LD. QA sonrası DB seed'e döndürüldü, admin_users temizlendi.
- Now: [→] Yayın öncesi insan işleri: avukat incelemesi, gerçek telefon/ETBİS, gerçek ürün verisi (artık backoffice'ten girilebilir)
- Remaining:
  - [ ] Phase 9 (site): cache header'ları, hata sayfaları, slug regresyon testleri, Dockerfile YERİNE systemd/launchd deploy notu, final QA

## Open Questions
- UNCONFIRMED: Gerçek ürün adları/fiyatları — seed'ler açıkça kurgusal placeholder
- UNCONFIRMED: microline.com.tr domain'i (base-url varsayımı)
- Yasal metinler avukat incelemesi bekliyor (yer tutucu olarak seed'lenecek)

## Working Set
- Dizin: /Users/felece/Dev/Microline (git: main)
- Plan: /Users/felece/.claude/plans/microline-website-blueprint-prancy-peach.md
- Test: `export JAVA_HOME=$(/usr/libexec/java_home -v 21) && ./mvnw test`
- Çalıştır: `./mvnw spring-boot:run -Dspring-boot.run.profiles=dev`
- DB: `psql -h localhost -U microline -d microline`
